cmake_minimum_required(VERSION 3.21)
project(ABWStorageTests LANGUAGES CXX)
enable_testing()
add_executable(StorageTests StorageTests.cpp ../src/FormListStorage.cpp)
target_compile_features(StorageTests PRIVATE cxx_std_23)
target_include_directories(StorageTests PRIVATE . ../include)
add_test(NAME StorageTests COMMAND StorageTests)

# Compile the actual private provisioning functions against an inventory double.
# No copied implementation: source edits regenerate this fixture automatically.
set(ABW_PROVISION_SOURCE "${CMAKE_CURRENT_SOURCE_DIR}/../src/FloaterSetup.cpp" CACHE FILEPATH "Provisioning source under test")
set_property(DIRECTORY APPEND PROPERTY CMAKE_CONFIGURE_DEPENDS "${ABW_PROVISION_SOURCE}")
file(READ "${ABW_PROVISION_SOURCE}" provisionSource)
string(FIND "${provisionSource}" "\t\tstruct ProvisionedWeaponState" provisionStart)
string(FIND "${provisionSource}" "\t\tvoid SleepThenAddTask(" provisionEnd)
if(provisionStart LESS 0 OR provisionEnd LESS provisionStart)
    message(FATAL_ERROR "Provisioning test extraction markers changed")
endif()
math(EXPR provisionLength "${provisionEnd} - ${provisionStart}")
string(SUBSTRING "${provisionSource}" ${provisionStart} ${provisionLength} provisionFunctions)
file(WRITE "${CMAKE_CURRENT_BINARY_DIR}/ProvisionFunctions.inc" "${provisionFunctions}")
add_executable(ProvisionTests ProvisionTests.cpp)
target_compile_features(ProvisionTests PRIVATE cxx_std_23)
target_include_directories(ProvisionTests PRIVATE . "${CMAKE_CURRENT_BINARY_DIR}")
add_test(NAME ProvisionTests COMMAND ProvisionTests)

# Exercise the production on-cast event sequence without starting Skyrim.
set(ABW_ACTIVATE_SOURCE "${CMAKE_CURRENT_SOURCE_DIR}/../src/Activate.cpp" CACHE FILEPATH "Activation source under test")
set_property(DIRECTORY APPEND PROPERTY CMAKE_CONFIGURE_DEPENDS "${ABW_ACTIVATE_SOURCE}")
file(READ "${ABW_ACTIVATE_SOURCE}" activateSource)
set(activateFunctions "")
foreach(pair IN ITEMS "struct OnCastCoalesce|void FinishOnCastSpawn" "bool HasFreshDualBoundEffect|RE::SpellItem* AssignableBoundOnPlayer")
    string(REPLACE "|" ";" markers "${pair}")
    list(GET markers 0 beginMarker)
    list(GET markers 1 endMarker)
    string(FIND "${activateSource}" "${beginMarker}" beginOffset)
    string(FIND "${activateSource}" "${endMarker}" endOffset)
    if(beginOffset LESS 0 OR endOffset LESS beginOffset)
        message(FATAL_ERROR "Activation test extraction markers changed")
    endif()
    math(EXPR sectionLength "${endOffset} - ${beginOffset}")
    string(SUBSTRING "${activateSource}" ${beginOffset} ${sectionLength} section)
    string(APPEND activateFunctions "${section}")
endforeach()
file(WRITE "${CMAKE_CURRENT_BINARY_DIR}/ActivateFunctions.inc" "${activateFunctions}")
add_executable(ActivateTests ActivateTests.cpp)
target_compile_features(ActivateTests PRIVATE cxx_std_23)
target_include_directories(ActivateTests PRIVATE . "${CMAKE_CURRENT_BINARY_DIR}")
add_test(NAME ActivateTests COMMAND ActivateTests)
